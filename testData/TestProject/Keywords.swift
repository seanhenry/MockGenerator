protocol Keywords {
    func run(for: Int)
    func `for`(in: String)
    func keywords1(while: Int, repeat: Int, switch: Int, case: Int, default: Int, do: Int, catch: Int, defer: Int)
/*
properties
lookup all keywords
keywords in func names and labels
multiple keywords in labels
*/
}
