protocol Keywords {
    func run(for: Int)
    func `for`(in: String)
//    func keyword(typealias: Int)
/*
properties
lookup all keywords
keywords in func names and labels
multiple keywords in labels
*/
}
